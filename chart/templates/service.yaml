apiVersion: v1
kind: Service
metadata:
  name: {{ include "sheerwater-chat.fullname" . }}
  labels:
    {{- include "sheerwater-chat.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sheerwater-chat.selectorLabels" . | nindent 4 }}
